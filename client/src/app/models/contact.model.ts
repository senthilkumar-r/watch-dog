export interface Contact {
    id: string;
    first_name: string;
    last_name: string;
    // gender: string;
    // email?: string;
    phone_number: string;
    // date_of_birth: Date;
    // avatar_url?: string;
    // created_date: Date;
    // updated_date: Date;
}